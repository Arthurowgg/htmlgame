# Launcher

Static web app (no build step). Entry point is the repo root `index.html`.

- `css/launcher.css` — Minecraft-inspired dark UI
- `js/launcher.js` — catalog, IndexedDB installs, play via blob URLs
